name: post5-exact-validation

on:
  push:
    branches:
      - remediation/v0.4.0-post5
    paths:
      - .github/workflows/post5-exact-validation.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: remediation/v0.4.0-post5
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Check out exact clean candidate worktree
        shell: bash
        run: git worktree add "$RUNNER_TEMP/post5-candidate" 4ae21f6c33f77bd73c1904637af0b56abc0e7106
      - name: Install exact candidate
        run: python -m pip install -e "$RUNNER_TEMP/post5-candidate[dev]"
      - name: Validate pinned candidate
        shell: bash
        run: |
          set +e
          E="$GITHUB_WORKSPACE/post5-exact-evidence"
          C="$RUNNER_TEMP/post5-candidate"
          mkdir -p "$E"
          cd "$C"

          python -m pytest -q \
            tests/test_v040_post5_remediation.py \
            tests/test_v040_post4_remediation.py \
            tests/test_v040_cognitive_foundations.py \
            tests/test_v040_remediation.py \
            tests/test_v040_post5_version.py > "$E/focused.txt" 2>&1
          focused=$?

          python -m pytest -q > "$E/full.txt" 2>&1
          full=$?
          python -m compileall -q app tests scripts > "$E/compile.txt" 2>&1
          compile=$?
          python -m ruff check app tests scripts > "$E/ruff.txt" 2>&1
          ruff=$?

          rm -rf .pytest_cache .ruff_cache
          find app tests scripts -type d -name '__pycache__' -prune -exec rm -rf {} +
          find . -type f \( -name '*.pyc' -o -name '*.pyo' \) -delete
          python - <<'PY' > "$E/cleanup.txt" 2>&1
          from pathlib import Path
          bad_dirs = {"__pycache__", ".pytest_cache", ".ruff_cache"}
          bad_suffixes = {".pyc", ".pyo"}
          leftovers = [
              path.as_posix()
              for path in Path('.').rglob('*')
              if any(part in bad_dirs for part in path.parts)
              or (path.is_file() and path.suffix in bad_suffixes)
          ]
          if leftovers:
              raise SystemExit(leftovers)
          print('generated-file cleanup passed')
          PY
          cleanup=$?

          python scripts/validate_package.py > "$E/source-package.txt" 2>&1
          source_package=$?
          rm -rf dist "$RUNNER_TEMP/post5-extracted"
          python scripts/build_release.py --output dist/embodied-alife-update.zip --app-id embodied-alife > "$E/build.txt" 2>&1
          build=$?
          mkdir -p "$RUNNER_TEMP/post5-extracted"

          python - <<'PY' > "$E/archive.txt" 2>&1
          import hashlib, json, os, zipfile
          from pathlib import Path, PurePosixPath
          path = Path('dist/embodied-alife-update.zip')
          data = path.read_bytes()
          digest = hashlib.sha256(data).hexdigest()
          print(f'sha256={digest}')
          print(f'checksum={digest}  embodied-alife-update.zip')
          print(f'size={len(data)}')
          with zipfile.ZipFile(path) as archive:
              if archive.testzip() is not None:
                  raise SystemExit('archive integrity failed')
              names = archive.namelist()
              if len(names) != len(set(names)):
                  raise SystemExit('duplicate members')
              unsafe = [n for n in names if PurePosixPath(n).is_absolute() or '..' in PurePosixPath(n).parts or '\\' in n]
              if unsafe:
                  raise SystemExit(f'unsafe={unsafe}')
              temporary = [n for n in names if 'post5-' in n.lower() or 'apply_phase' in n.lower()]
              if temporary:
                  raise SystemExit(f'temporary={temporary}')
              manifest = json.loads(archive.read('update-manifest.json'))
              expected = sorted(n for n in names if n != 'update-manifest.json')
              if sorted(manifest['managed_paths']) != expected:
                  raise SystemExit('managed path mismatch')
              if manifest['version'] != '0.4.0.post5' or manifest['app_id'] != 'embodied-alife':
                  raise SystemExit(manifest)
              if manifest['preserved_roots'] != ['.env', '.venv', 'data', '.git']:
                  raise SystemExit(manifest['preserved_roots'])
              print(f'member_count={len(names)}')
              print('manifest=' + json.dumps(manifest, sort_keys=True, separators=(',', ':')))
              for name in names:
                  print(name)
              archive.extractall(Path(os.environ['RUNNER_TEMP']) / 'post5-extracted')
          PY
          archive=$?

          (cd "$RUNNER_TEMP/post5-extracted" && python scripts/validate_package.py) > "$E/extracted-package.txt" 2>&1
          extracted=$?

          cp dist/embodied-alife-update.zip "$E/"
          sha256sum dist/embodied-alife-update.zip > "$E/sha256.txt"
          git rev-parse HEAD > "$E/candidate-commit.txt"
          git status --short > "$E/candidate-status.txt"

          overall=0
          for value in "$focused" "$full" "$compile" "$ruff" "$cleanup" "$source_package" "$build" "$archive" "$extracted"; do
            if [ "$value" -ne 0 ]; then overall=1; fi
          done
          cat > "$E/status.txt" <<EOF
          candidate=4ae21f6c33f77bd73c1904637af0b56abc0e7106
          focused=$focused
          full=$full
          compile=$compile
          ruff=$ruff
          cleanup=$cleanup
          source_package=$source_package
          build=$build
          archive=$archive
          extracted=$extracted
          overall=$overall
          EOF
          exit 0
      - name: Commit textual evidence
        shell: bash
        run: |
          rm -f post5-exact-evidence/embodied-alife-update.zip
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add post5-exact-evidence
          git commit -m "Record pinned post5 candidate validation evidence" || true
          git push origin HEAD:remediation/v0.4.0-post5
      - name: Upload archive and evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post5-exact-4ae21f6-evidence
          path: post5-exact-evidence
          if-no-files-found: error
      - name: Enforce result
        shell: bash
        run: grep -q '^overall=0$' post5-exact-evidence/status.txt
