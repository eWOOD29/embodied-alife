name: post5-apply-phase6

on:
  push:
    branches:
      - remediation/v0.4.0-post5
    paths:
      - scripts/post5_apply_phase6.py
      - .github/workflows/post5-apply-phase6.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: remediation/v0.4.0-post5
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Apply phase 6
        run: python scripts/post5_apply_phase6.py
      - name: Commit phase 6
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/diagnostics.py
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Remove accidental source null byte"
            git push origin HEAD:remediation/v0.4.0-post5
          fi
