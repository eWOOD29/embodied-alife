name: post5-apply-phase9

on:
  push:
    branches: [remediation/v0.4.0-post5]
    paths:
      - scripts/post5_apply_phase9.py
      - .github/workflows/post5-apply-phase9.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: remediation/v0.4.0-post5
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - run: python scripts/post5_apply_phase9.py
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/simulation/agent.py app/simulation/world.py tests/test_v040_remediation.py
          git commit -m "Adapt legacy regressions to validated provenance" || exit 0
          git push origin HEAD:remediation/v0.4.0-post5
